# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
.venv/
venv/
uv.lock

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Project — large data, never committed.
# Paths are listed without trailing slash so the patterns match both real
# directories and symlinks-into-native-fs (see DESIGN.md "WSL2 storage
# convention").
data/raw
data/public
data/processed
models/checkpoints
models/exported
db/*.sqlite
db/*.sqlite-journal
db/*.sqlite-wal
db/*.sqlite-shm

# Catalog artifacts — regenerable from NHTSA vPIC via `build-catalog`.
catalog/classes.json
catalog/cache/
