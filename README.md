# Car Lense

Offline data + training pipeline for the Car Lense vehicle-recognition app.

See [`DESIGN.md`](./DESIGN.md) for architecture and [`TODO.md`](./TODO.md) for progress.

## Quickstart

```
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
pytest
```
