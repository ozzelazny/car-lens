# Car Lense — Top-level TODO

See `DESIGN.md` for architecture. This file tracks high-level progress across the whole project.

## Current focus

Engine v1 shipped: MobileCLIP-B fine-tuned on 360k images / 6,423 US-skewed classes, test top-1=81.07% / top-5=98.10%. iOS MVP scaffold under `app/ios/CarLense/`. Recognize service live (`services/recognize_api/`) with binary exterior gate + optional LLM re-rank (Phase 6.2). Next: build iOS app on a Mac and install on device. BaT crawler ingesting more real-world data in background.

## Phase 1 — Catalog and crawler infrastructure

- [x] **1.1** Project scaffold (pyproject.toml, src/ layout, .gitignore, ruff config, pytest config)
- [x] **1.2** SQLite schema + DB access layer (listings, images, crawl_queue, dedupe)
- [x] **1.3** NHTSA vPIC catalog builder — pull canonical (year, make, model) list, cache locally
- [x] **1.4** Search-query generator — produce per-site search URLs for top-N (make, model, year) combos
- [x] **1.5** Crawler core — Playwright + stealth, request queue, retry/backoff, rate-limit, resume-after-restart

## Phase 2 — Per-site parsers

- [x] **2.1** cars.com listing parser + image extractor
- [x] **2.2** AutoTrader listing parser + image extractor
- [x] **2.3** Craigslist listing parser (free-text title → structured label)
- [x] **2.4** Bring a Trailer / Hemmings / Cars & Bids parsers

## Phase 3 — Image pipeline

- [x] **3.1** Image downloader (curl_cffi, browser-fingerprint TLS)
- [ ] **3.2** pHash near-duplicate detection
- [x] **3.3** CLIP zero-shot **view + content labeling** — per image, assign `(view ∈ {front, rear, side, three-quarter-front, three-quarter-rear, interior, detail, non-car}, score)`. Drop non-car. Keep interior/detail rows in DB (excluded from training) so a v1.1 interior path is possible without re-crawling. *Infra (migration 003 + `view-label` CLI + `view_labeler.py`) shipped earlier and ran on crawled rows (bat/cars_com/craigslist, 4,654 rows). Phase 3.3 closed 2026-05-17 by labeling the 152,349 remaining public-dataset rows (compcars 136k + stanford_cars 16k) with ViT-L-14/laion2b_s32b_b82k on CUDA RTX 5090, ~108 img/s, 23 min. Distribution: three-quarter-front 27.8%, side 27.2%, three-quarter-rear 21.2%, front 16.7%, rear 7.0%, interior 0.03%, non-car 0.01%, detail 0.01% — straight rear shots are underrepresented across the public corpus; only 77 non-exterior rows in the entire 152k batch (very clean datasets).*
- [ ] **3.4** Quality filter (resolution, blur, aspect ratio) — applied after view labeling.
- [x] **3.5** Train / val / test split **stratified by (class, view)** — no view leaks across splits. Exterior-only for v1. *Migration 010 added `images.split` (per-image instead of per-listing because the same listing can have multiple views). New `make-splits` CLI lives in `src/car_lense_engine/dataset/make_splits.py` with listing-coherent stratification (default ON — all images of one listing land in the same split, preventing same-car leakage). Baseline + training readers (`baseline.py`, `train_classifier.py`) switched from `listings.split` to `images.split` and now plumb `images.view` through the row tuples for Phase 6.1 view-conditional prototypes. CompCars run 2026-05-17: 136,232 eligible rows → 114k train / 10.7k val / 10.8k test, with 28k rows in small `(class, view)` cells routed all-to-train (sparse buckets — expected with 2,566 classes × 5 views = 12,830 possible cells). per_class_p10 = 9 images. 661 unit tests pass.*

## Phase 4 — Public datasets

- [x] **4.1** Stanford Cars downloader + label normalizer — ingest verified end-to-end against `Multimodal-Fatima/StanfordCars_train` (HF mirror, int ClassLabel decoded via `ds.features['label'].int2str`).
- [x] **4.2** VMMRdb downloader + label normalizer — ingest module + CLI + tests built against `venetis/VMMRdb_make_model_{train,val,test}` (the only working HF mirror). HF mirror is **make_model only** (375 classes, no year), not the full 9,170-class year-make-model release. The label parser handles BOTH formats (year-suffix `honda_civic_2005` and no-year `acura_cl`); when ingesting from the venetis mirror, `listings.year` ends up `NULL`. Live ingest not yet run end-to-end against the live mirror — verify with `import-vmmrdb --limit 50` before kicking off the full run.
- [x] **4.3** CompCars downloader + label normalizer — ingest module + CLI + 33 tests against `JorgeLlorente/CompCars-Repository/Compcars_Data.zip` (16.5 GB ZIP, no `datasets`-streaming API). Custom path: download via direct curl (HF Xet kept rewinding chunks), iterate locally with `zipfile.ZipFile`, resolve integer make_id/model_id via `scipy.io.loadmat` on `misc/make_model_name.mat` (163 makes / 2004 models). `car_type.mat` in the live release ships only a `types` field — no model_type lookup — so `body_style` is always NULL for compcars rows. Live 20-row smoke verified end-to-end: year populated, sensible make/model labels. **ZIP cached on disk at `data/public/compcars/Compcars_Data.zip`; full ingest can run with `import-compcars --zip-path ...` (no re-download).**
- [ ] **4.4** Wikimedia Commons fetcher (vintage gap)
- [x] **4.5** Unified label schema across all sources — canonical_make / canonical_model columns (migration 008) + hand-curated alias map (BWM→BMW, Benz→Mercedes-Benz, Chevy→Chevrolet, Buck→Buick, lowercase→Title-Case) + Title-Case fallback. 152,848 rows canonicalized into 170 makes / 4,762 (year, make, model) classes. Phase 5 baseline + training pipelines now SELECT canonical fields. `canonicalize-labels` CLI is idempotent.

## Phase 5 — Model training (not yet planned in detail)

- [x] **5.1** Baseline: pre-trained MobileCLIP-S2 zero-shot prototype retrieval. Stanford Cars (196 classes, 8,014 test images): **top-1=87.88%, top-3=98.74%, top-5=99.64%, top-10=99.90%**, ~9 min on CPU. Per-view conditioning deferred to 5.2 — single-prototype baseline established first per industry convention.
- [x] **5.2** Fine-tune MobileCLIP-S2 with hard-negative-weighted CE. Two runs: (a) Stanford Cars 196 classes, 20 epochs on RTX 5090 (~19 min): **top-1=91.48%, top-5=98.85%** (vs zero-shot baseline 87.88%/99.64%); (b) **bucketed CompCars 2,566 classes, 10 epochs, ~74 min**: **top-1=91.93%, top-5=99.24%** — better accuracy on 13× more classes, courtesy of Phase 4.6 year-bucketing + 122k train images + hard-negative weighting. View-conditional retrieval still pending Phase 3.5 view-stratified split. **2026-05-18 combined retrain on compcars+vmmrdb+stanford_cars (6,423 classes, 360k train images)** — MobileCLIP-S2 5 epochs val=76.94 / test=63.84; **MobileCLIP-B 5 epochs val=83.87 / test=75.67; MobileCLIP-B resumed +10 epochs val=85.66 / test=81.07 / top-5=98.10**. Backbone swap S2→B alone gave +11.8 pp test top-1; resume-and-continue gave another +5.4 pp; val/test gap compressed 13pp→4.6pp.
- [x] **5.3** Train the **view classifier** (small head on the same backbone over 5 exterior views + 1 "non-exterior" class). *Code shipped 2026-05-17: `src/car_lense_engine/training/view_classifier.py` (frozen-backbone + cached-features trainer over 6 classes — front/rear/side/three-quarter-front/three-quarter-rear/non-exterior, with `non-exterior` collapsing interior/detail/non-car), `view_classifier_cli.py` (entry point `train-view-classifier`), 15 unit tests. First run 2026-05-17: 85,543 train / 8,229 val rows from CompCars, 15 epochs, ~9 min on RTX 5090 (mostly feature caching): **val_top1 = 72.5%** at epoch 14. Loss still trending down — more epochs would likely help. Class weights inverse-sqrt: front=1.15, rear=1.81, side=0.93, 3Q-front=1.05, 3Q-rear=1.06, **non-exterior=0.00** because `make-splits` excludes non-exterior rows from `images.split` (they stay NULL and never reach the trainer). Practical consequence: the model effectively learned only the 5 exterior classes. Non-exterior rejection will rely on `view_reject_threshold` (low-confidence softmax) rather than a learned class — to learn it properly we'd need to extend `make-splits` to also assign splits to interior/detail/non-car rows, or have the view trainer use its own row selection that ignores `images.split` for non-exterior rows. Checkpoint at `models/checkpoints/view_classifier_v1.pt`.*
- [x] **5.4** Evaluation harness — held-out test set, top-1/top-5 broken down by `(make, view, era)`; full confusion matrix; per-view accuracy gap analysis. *Shipped 2026-05-17: `src/car_lense_engine/eval/evaluate.py` + `evaluate_cli.py` (entry `evaluate-recognize`), 9 unit tests. **CompCars-only test (10,816 images, 2,565 classes, 77 s): top-1 = 93.48%, top-3 = 99.17%, top-5 = 99.54%, top-10 = 99.74%.** **Combined test (37,782 images, 6,423 classes, 190 s) 2026-05-18: top-1 = 63.84%, top-3 = 89.13%, top-5 = 93.52%, top-10 = 96.87%** — top-1 regression vs CompCars-only is purely the 2.5× larger class space; top-5 still strong, exactly the regime LLM re-rank exploits. Worst view (combined): `side` 59.5% (vs 3Q-rear 73.3%). Worst era: `1970s` 47.1% (limited vintage training data). Top confusion pairs are within-make adjacent year-buckets (Impala 04-07 vs 08-11, Expedition 00-03 vs 96-99) and trim variants (Civic vs Civic_Coupe, Mustang vs Mustang_GT) — exactly the failure mode LLM re-rank addresses.*
- [x] **5.5** ONNX export (embedder + view classifier) + Core ML + TFLite conversion. *Shipped 2026-05-18. `src/car_lense_engine/export/mobile.py` auto-detects input size + normalize from the actual OpenCLIP preprocess transform (MobileCLIP-B = 224x224 [0,1]; S2 = 256x256 [0.5,0.5]). PyTorch -> ONNX (`torch.onnx.export(..., dynamo=False)`, opset 17, then `onnxsim`), `coremltools` -> `.mlpackage` (FP16), `onnx2tf` -> `.tflite` w/ ORT Mobile fallback. Bundles include FP16 `prototypes.bin` (raw little-endian, no on-device torch), `class_names.json`, `preprocessing.json`. Validator (`validate-mobile-export`) parity-checks cosine + top-1/5 identity against PyTorch. Heavy converters (`coremltools`, `onnx2tf`) gated behind `.[mobile]` extra. **Production export 2026-05-18:** `dist/ios/` = `model.mlpackage` (165 MB Core ML FP16) + `prototypes.bin` (6.3 MB, 6423 x 512) + `class_names.json` (390 KB) + `preprocessing.json` (421 B) = 172 MB total. PyTorch vs ONNX parity: cos mean=1.000000, top-1+top-5 identity 20/20. Android TFLite blocked by an `onnx2tf` native-code segfault — deferred (user has iPhone only); ONNX (`dist/common/model.onnx`, 345 MB) retained for an ORT-Mobile fallback path.*
- [ ] **5.6** On-device latency benchmark (embedder ≤ 30 ms target, view classifier ≤ 5 ms target).

## Phase 6 — `recognize()` engine interface (not yet planned in detail)

- [x] **6.1** `recognize()` Python interface — view-detect → view-conditional retrieval → top-K candidates. Reject non-exterior inputs with a clear reason. *Phase 6.1 closed 2026-05-17. `build_prototypes_by_view` added to `eval/baseline.py` producing schema-v2 cache at `cache/prototypes_by_view.pt` (26 MB, 2,566 classes × 5 views × 512 dim; populated coverage per view: front 92.5%, rear 80.7%, side 97.8%, 3Q-front 95.0%, 3Q-rear 94.7%). `services/recognize_api/app.py` extended with `VIEW_CLASSIFIER_PATH` + `VIEW_REJECT_THRESHOLD` env vars; loader auto-detects v1 vs v2 prototype cache; `/api/recognize` runs view classification first, returns HTTP 422 `{detail, view, view_score}` on non-exterior or below-threshold predictions, else retrieves against the matching view's prototypes. `/health` reports `view_classifier_loaded` + `views_with_prototypes`. Response carries `view` + `view_score` fields. **End-to-end smoke verified** against a CompCars 3Q image: backbone + view classification + per-view top-5 retrieval = ~750 ms on RTX 5090. **Threshold caveat:** with the current 72.5% view classifier the default `VIEW_REJECT_THRESHOLD=0.5` is too aggressive (correct predictions average ~25-46% softmax); set to `0.15` for current production use until the view classifier is retrained for higher confidence. Single-prototype fallback (v1 cache, no `VIEW_CLASSIFIER_PATH`) still works for backwards compat. 747 unit tests pass.*
- [~] **6.2** Cloud-LLM re-rank fallback for low-confidence top-1 (threshold TBD post-eval). *Code shipped 2026-05-18: `services/recognize_api/llm_rerank.py` wraps Anthropic Messages API (image resize to 768px, JPEG q85, base64; system prompt asks for a single integer pick; parses + bounds-checks); never raises — all failures preserve original CLIP top-K. `services/recognize_api/app.py` extended with `LLM_RERANK_ENABLED` / `LLM_RERANK_THRESHOLD` / `LLM_RERANK_MODEL` / `LLM_RERANK_TIMEOUT` / `ANTHROPIC_API_KEY` env vars; triggers only when top-1 confidence < threshold (default 0.5). Response gains `rerank_applied` + `rerank_latency_ms` fields; `/health` reports `llm_rerank_enabled` + `llm_rerank_model`. 12 unit tests + 8 API tests stub the SDK. **Awaiting user-provided ANTHROPIC_API_KEY to enable in production** — math says effective top-1 ~92-94% with re-rank on top of the current 98.10% top-5.*
- [ ] **6.3** CLI for one-shot inference + batch evaluation.

## Known follow-ups (non-blocking)

- Catalog Title Case loses canonical capitalization for compound names: `"MCLAREN"` → `"Mclaren"`, not `"McLaren"`; same for `"BMW"` (becomes `"Bmw"`). Acceptable for taxonomy + matching, but the app's display layer will need a canonical-name override table eventually.
- BaT name-parse doesn't recognize two-word *models* like `"Del Sol"`, `"Type R"`. Two-word-make matching exists (Land Rover, Alfa Romeo) but no symmetric two-word-model logic. A `1997 Honda Del Sol` listing comes out as `model="Del", trim="Sol Si 5-Speed"`. Affects a handful of model families.
- `parse_proxy_url` still echoes the raw URL in the `"missing scheme"` `ValueError` (e.g. `//user:pass@host:8080` → message contains credentials). Triggered only by schemeless input, which is unusual but possible. The missing-host and missing-port paths were fixed in `b390fa4`; this third path remains as the only residual leak surface.
- Crawler smoke results are non-deterministic against Cloudflare-protected sites (cars.com observed flipping 200→403 between identical runs). Validation of parser fixes for those sites requires either consistent access via residential proxy OR a fixture-based test against saved HTML snapshots.
- AutoTrader vehicle-detail pages return an Akamai Bot Manager interstitial (HTTP 200, ~3.7 KB, title "Autotrader - page unavailable", assets under `/akamai-block/...`) when fetched via `CurlCffiFetcher(impersonate="chrome131")`. Sitemap URL discovery still works, but no detail data is extractable without a residential proxy. Fixture preserved at `tests/crawler/parsers/fixtures/real_world/autotrader_detail_curlcffi_chrome131_20260515T231622Z.html`. Reopen for parser work when a proxy pool is provisioned.
- Stanford Cars HF mirror (`Multimodal-Fatima/StanfordCars_train`) emits class strings lowercased (e.g. `"acura tl sedan 2012"`, not `"Acura TL Sedan 2012"`). Our parser preserves the casing it receives, so `listings.make` for `source='stanford_cars'` is lowercase while crawled sources store Title Case. Phase 4.5 (unified label schema) needs a normalize-on-save pass (or a case-insensitive catalog matcher) before training joins make labels across sources.
- Migration 004 (rebuild `listings` to widen `source` CHECK) has a narrow data-loss window between `DROP TABLE listings` and `ALTER TABLE listings_new RENAME TO listings` — if a crash hits in that gap, the retry's `DROP TABLE IF EXISTS listings_new` would destroy the only copy. Safer pattern: rename old → rename new → drop old. Two-statement gap with no I/O between, so probability is tiny; close in a follow-up. *Status: migration 006 (Phase 4.2) demonstrates the safer pattern — adds `PRAGMA legacy_alter_table = ON` to prevent SQLite's auto-rewriting of FKs in `images` when `listings` is RENAMEd. Migration 004 not retroactively rewritten (already-applied DBs would skip a fix anyway).*
- `make-splits` listing-coherent path leaks into the test bucket when `train_frac + val_frac == 1.0` (e.g. train_frac=1.0, val_frac=0.0). Reviewer-flagged 2026-05-17: `train_count` is updated AFTER the `<` check, so a single oversized listing overshoots `n_train_target` and the next listing falls through to the test bucket. The image-level path is fine because positional slicing with `n_train=n_total` covers every index. Doesn't trigger for the production 0.8/0.1/0.1 config. Fix: when remaining train/val quota is saturated AND `train_frac + val_frac >= 1.0 - eps`, route to whichever bucket is closest to its target. Add a `train_frac=1.0` unit test.
- VMMRdb HF mirror (`venetis/VMMRdb_make_model_*`) is the only working HuggingFace upload as of 2026-05; it has **375 make-model classes (no year)**, not the full 9,170-class year-make-model VMMRdb. To get the full year-tagged dataset we'd need to download the original GitHub release (~50 GB) and ingest from disk. Closes the immediate Phase 4.2 ship gate (~100k images, ~375 classes); year-tagged ingest path deferred. Listing rows ingested from the venetis mirror will have `listings.year IS NULL`; Phase 4.5 (unified label schema) and the training-time stratified split need to account for this.

## Status legend

- `[ ]` pending
- `[~]` in progress
- `[x]` done
